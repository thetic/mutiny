@PACKAGE_INIT@

list(APPEND CMAKE_MODULE_PATH "@PACKAGE_MODULES_DIR@")
set_and_check(mutiny_INCLUDE_DIRS "@PACKAGE_INCLUDE_DIR@")
if(NOT TARGET mutiny)
  include("${CMAKE_CURRENT_LIST_DIR}/mutinyTargets.cmake")
endif()

set(mutiny_LIBRARIES mutiny::mutiny)

check_required_components(mutiny)
